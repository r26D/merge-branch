{
  "name": "merge-branch",
  "version": "2.0.0",
  "description": "A GitHub Action that manages git merge for any gitflow",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "vitest": "^3.1.1"
  },
  "private": true
}
